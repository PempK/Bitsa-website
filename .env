# This is a placeholder for environment variables.
# The VITE_OAUTH_PORTAL_URL is required for the application to run without the "Invalid URL" error.
# Replace the placeholder with your actual OAuth server URL.

VITE_OAUTH_PORTAL_URL=http://localhost:3000
VITE_APP_ID=bitsa-app
